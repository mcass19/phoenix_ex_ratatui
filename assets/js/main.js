// Phoenix LiveView Hook for `PhoenixExRatatui.LiveView` /
// `PhoenixExRatatui.LiveComponent`.
//
// Filled in in a follow-up chunk. This skeleton exports an empty Hook
// object so esbuild has something to bundle and CI's "verify JS bundle
// is in sync" step has something to compare against.

export const PhoenixExRatatuiHook = {
  mounted() {},
  updated() {},
  destroyed() {},
};

export default PhoenixExRatatuiHook;
